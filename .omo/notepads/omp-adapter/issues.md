- No contract shape required a supplemental runtime probe; all event, result, bash-input, content-union, and exports-map data resolved from installed declarations/package metadata. The session identity runner test is mandatory v4 evidence rather than a type-extraction fallback, and it passed.
- D10 expands five optional `BashToolInput` keys (`async`, `cwd`, `env`, `pty`, `timeout`) around required `command`, producing 32 exact field-to-primitive-type variants. `env` is recorded as the primitive category `object`.
- The local GitNexus wrapper could not run because `/home/trav/.local/bin/gitnexus` is absent, and no GitNexus MCP server was exposed in this session. The change is isolated to new unindexed files under `tests/runtime/omp/`.
- The external programming-skill no-excuse checker could not resolve the workspace's installed `typescript@6.0.2`; a direct strict `bunx tsc --noEmit` pass over all non-`bun:test` scripts succeeded instead. The test file remains exercised by Bun's native compiler/runtime.
